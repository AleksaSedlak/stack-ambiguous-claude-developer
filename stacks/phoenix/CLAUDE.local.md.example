# Personal Overrides

> Rename this file to `CLAUDE.local.md` to activate it. It's gitignored — your settings stay local.

## Local Dev Environment

- DB runs on `localhost:5432`, user `postgres`, no password
- Dev server on port 4000: `iex -S mix phx.server`

## My Preferences

- I prefer concise responses — skip the recap after each change
- When suggesting fixes, show the diff, don't explain what changed

## Current Work

- Working on the billing context refactor (branch: `feature/billing-v2`)
- Don't touch `lib/my_app/legacy/` — that's being removed next sprint
